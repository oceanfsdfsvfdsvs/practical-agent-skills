# OpenClaw Notes

Install by copying the `feature-flag-debt-audit/` directory into the OpenClaw workspace skills directory.

Suggested check when the CLI is available:

```bash
openclaw skills check feature-flag-debt-audit
```

This workspace did not verify the skill through OpenClaw unless that command succeeds locally. The skill itself is local-first and does not require network access for its bundled smoke test.
