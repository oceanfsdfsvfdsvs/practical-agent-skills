# OpenClaw Compatibility Notes

Install by copying the full `tax-notice-response-preflight` directory into your OpenClaw workspace skills directory so `SKILL.md`, `references/`, `templates/`, `examples/`, and `scripts/` stay together.

Suggested local check, when supported by your OpenClaw CLI:

```bash
openclaw skills check tax-notice-response-preflight
```

Status: `not-verified-locally`

The OpenClaw CLI was not available in this automation environment, so this run verified the Markdown skill structure and local Python fixture only.
