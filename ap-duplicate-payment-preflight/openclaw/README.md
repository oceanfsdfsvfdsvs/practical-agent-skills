# OpenClaw Notes

This skill is local-first and can be installed by copying the `ap-duplicate-payment-preflight/` directory into the OpenClaw workspace skills directory.

Suggested manual check when the OpenClaw CLI is available:

```bash
openclaw skills check ap-duplicate-payment-preflight
```

Runtime status for this workspace: not locally verified in this run unless the command above succeeds. Do not claim OpenClaw registry or ClawHub support until the skill is installed and checked in the target environment.
